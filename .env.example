# TokenTriage environment configuration
# Copy to .env and fill in. NEVER commit real keys (rubric hard rule).
# .env is gitignored; this .example file is the committed template.

# --- Phase 1: fully local, NO keys needed --------------------------------
# Tiers run on local open-source models via Ollama (see models/registry.py).
# Just have Ollama running with the models pulled (one per command):
#   ollama pull qwen2.5:3b
#   ollama pull qwen2.5:7b
#   ollama pull qwen2.5:14b
#   ollama pull nomic-embed-text
OLLAMA_HOST=http://localhost:11434
TOKENTRIAGE_EMBED_PROVIDER=ollama            # local embeddings for the T0 cache
TOKENTRIAGE_OLLAMA_EMBED_MODEL=nomic-embed-text

# --- Phase 2/3: add cloud tiers (optional) -------------------------------
# Point a tier at a cloud provider in models/registry.py, then set its key.
# One shared key powers any cloud tier; per-tier vars override it (isolation).
# GEMINI_API_KEY=your-gemini-api-key-here     # https://aistudio.google.com/apikey
# OPENAI_API_KEY=your-openai-api-key-here
# TOKENTRIAGE_T3_API_KEY=                      # optional per-tier override
# To make Gemini a live backend tier, edit src/tokentriage/models/registry.py:
#   provider="gemini", model_id="gemini-2.5-flash", api_key_env="TOKENTRIAGE_T3_API_KEY"
# Note: If a cloud tier is configured in registry.py but its key is missing here,
# the orchestrator will automatically disable it and safely fallback to local models.
# The Gemini-compatible ingress works either way:
#   POST /v1beta/models/{model}:generateContent

# --- Server ---------------------------------------------------------------
TOKENTRIAGE_HOST=0.0.0.0
TOKENTRIAGE_PORT=8000

# --- Storage --------------------------------------------------------------
TOKENTRIAGE_DB_PATH=tokentriage.db

# --- Policy ---------------------------------------------------------------
TOKENTRIAGE_POLICY_PATH=config/policy.yaml
